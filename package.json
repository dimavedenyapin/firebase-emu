{
  "name": "firebase-emu-rs",
  "version": "0.1.4",
  "description": "Local Firestore, Auth, Storage, Pub/Sub, Functions, and emulator UI implemented in Rust",
  "license": "Apache-2.0",
  "repository": "github:dimavedenyapin/firebase-emu",
  "bin": {
    "firebase-emu": "bin/firebase-emu.cjs"
  },
  "scripts": {
    "test": "node --test test/launcher.test.cjs"
  },
  "files": [
    "bin/firebase-emu.cjs"
  ],
  "engines": {
    "node": ">=18"
  }
}
