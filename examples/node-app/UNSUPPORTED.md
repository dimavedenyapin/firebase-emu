# Test scope

All 19 required Node SDK checks must pass on Google and Rust. The listener check includes a live update. No required operation is skipped.

Firestore field transforms, aggregation, partition queries, Security Rules, and production transaction isolation are outside this example. Storage multi-chunk retry and IAM are outside this example. See the root README for local emulator limits.
